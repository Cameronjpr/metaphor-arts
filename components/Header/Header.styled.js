import styled from 'styled-components'

export const StyledHeader = styled.header`
  @media (min-width: 1080px) {
    grid-column: 2;
    grid-row: 2 / span 2;
    text-align: left;
    margin: 0;
  }
  text-align: center;
  align-self: start;
  margin-top: 1em;
`
